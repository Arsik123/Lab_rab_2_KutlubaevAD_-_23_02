<Window x:Class="Lab_rab_2_KutlubaevAD_БПИ_23_02.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение сортировок — Thread vs Task (Кутлубaев А.Д.)"
        Height="820" Width="780" MinWidth="640" MinHeight="640">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">
            
            <GroupBox Header="Параметры" Margin="0,0,0,8" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="140"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Размер массива:"
                           VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="4,4,4,4"
                             Text="{Binding ArraySize, UpdateSourceTrigger=PropertyChanged}"
                             VerticalAlignment="Center"/>
                    <Button Grid.Row="0" Grid.Column="2" Content="🔀 Сгенерировать"
                            Command="{Binding GenerateArrayCommand}"
                            Margin="4" Height="28"/>

                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                Orientation="Horizontal" Margin="0,4,0,0">
                        <Label Content="Потоков (Thread):" VerticalAlignment="Center" Foreground="Blue"/>
                        <ComboBox ItemsSource="{Binding ThreadCounts}"
                                  SelectedItem="{Binding ThreadCount}"
                                  Width="45" Margin="4,0" VerticalAlignment="Center"/>
                        <Label Content="Тасков (Task):" VerticalAlignment="Center" Foreground="Green" Margin="8,0,0,0"/>
                        <ComboBox ItemsSource="{Binding TaskCounts}"
                                  SelectedItem="{Binding TaskCount}"
                                  Width="45" Margin="4,0" VerticalAlignment="Center"/>
                        <CheckBox Content="Общий массив (Задание 4)"
                                  IsChecked="{Binding UseSharedArray}"
                                  VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <Button Grid.Row="1" Grid.Column="2" Content="⛔ Остановить всё"
                            Command="{Binding StopAllCommand}"
                            Background="Red" Foreground="White"
                            Margin="4,4,4,0" Height="26"/>
                </Grid>
            </GroupBox>

            <TextBlock Text="{Binding OriginalArrayString}" TextWrapping="Wrap"
                       Margin="4,0,4,8" Foreground="#333"
                       FontFamily="Consolas" FontSize="11"/>

            <TextBlock Text="▸ Запуск через Thread:" FontWeight="SemiBold"
                       Margin="4,0,4,2" Foreground="Blue"/>
            <WrapPanel Margin="0,0,0,4">
                <Button Content="▶ Пузырьковая [Thread]"  Command="{Binding BubbleSortCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Быстрая [Thread]"       Command="{Binding QuickSortCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Вставками [Thread]"     Command="{Binding InsertionSortCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Шейкерная [Thread]"     Command="{Binding ShakerSortCommand}"
                        Width="180" Height="30" Margin="4"/>
            </WrapPanel>

            <TextBlock Text="▸ Запуск через async Task:" FontWeight="SemiBold"
                       Margin="4,0,4,2" Foreground="Green"/>
            <WrapPanel Margin="0,0,0,8">
                <Button Content="▶ Пузырьковая [Task]"  Command="{Binding BubbleSortTaskCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Быстрая [Task]"       Command="{Binding QuickSortTaskCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Вставками [Task]"     Command="{Binding InsertionSortTaskCommand}"
                        Width="180" Height="30" Margin="4"/>
                <Button Content="▶ Шейкерная [Task]"     Command="{Binding ShakerSortTaskCommand}"
                        Width="180" Height="30" Margin="4"/>
            </WrapPanel>

            <GroupBox Header="Пузырьковая сортировка" Margin="0,0,0,6" Padding="6">
                <StackPanel>
                    <TextBlock Text="Thread:" FontSize="10" Foreground="Blue" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding BubbleProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#2980B9"/>
                    <TextBlock Text="{Binding BubbleSortResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11" Margin="0,0,0,4"/>

                    <TextBlock Text="Async Task:" FontSize="10" Foreground="Green" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding BubbleTaskProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#27AE60"/>
                    <TextBlock Text="{Binding BubbleTaskResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Быстрая сортировка" Margin="0,0,0,6" Padding="6">
                <StackPanel>
                    <TextBlock Text="Thread:" FontSize="10" Foreground="Blue" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding QuickProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#2980B9"/>
                    <TextBlock Text="{Binding QuickSortResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11" Margin="0,0,0,4"/>

                    <TextBlock Text="Async Task:" FontSize="10" Foreground="Green" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding QuickTaskProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#27AE60"/>
                    <TextBlock Text="{Binding QuickTaskResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Сортировка вставками" Margin="0,0,0,6" Padding="6">
                <StackPanel>
                    <TextBlock Text="Thread:" FontSize="10" Foreground="Blue" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding InsertionProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#2980B9"/>
                    <TextBlock Text="{Binding InsertionSortResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11" Margin="0,0,0,4"/>

                    <TextBlock Text="Async Task:" FontSize="10" Foreground="Green" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding InsertionTaskProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#27AE60"/>
                    <TextBlock Text="{Binding InsertionTaskResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Шейкерная сортировка (Задание 1)" Margin="0,0,0,6" Padding="6">
                <StackPanel>
                    <TextBlock Text="Thread:" FontSize="10" Foreground="Blue" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding ShakerProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#2980B9"/>
                    <TextBlock Text="{Binding ShakerSortResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11" Margin="0,0,0,4"/>

                    <TextBlock Text="Async Task:" FontSize="10" Foreground="Green" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding ShakerTaskProgress}" Maximum="100"
                                 Height="14" Margin="0,0,0,2" Foreground="#27AE60"/>
                    <TextBlock Text="{Binding ShakerTaskResult}" TextWrapping="Wrap"
                               FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
            </GroupBox>

            <TextBlock Text="{Binding TotalComparisons}"
                       FontWeight="Bold" FontSize="13" Margin="4,4,4,4"/>

            <TextBlock Text="{Binding SharedArrayNote}"
                       Foreground="#AA3300" TextWrapping="Wrap"
                       FontSize="11" Margin="4,0,4,8"/>

        </StackPanel>
    </ScrollViewer>
</Window>
